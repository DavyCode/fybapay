// ServerResponse viewTransaction();

// ServerResponse viewTransactionByTransactionNumber(String transactionNumber);

// ServerResponse viewTransactionUser(String userId);

// ServerResponse viewTransactionInsight(String userId);

// ServerResponse viewTransactionRange(long startDate, long endDate);

// ServerResponse viewTransactionInsight(String userId, int pageNumber, ServiceInsightQueryDto request);

// ServerResponse viewTransactionUser(String userId, int page);

// ServerResponse viewTransactionByUserAndTypeAndStatusAndDate(String loggedInUserId, String userId, TransactionType type, TransactionStatus status, long startDate, long endDate, int page);

// ServerResponse viewTransactionByTypeAndStatusAndDateInsight(TransactionType type, TransactionStatus status, double amount, String transactionNumber, String name, long startDate, long endDate, int page, int limit);

// ServerResponse viewTransactionSearch(String search, int pageNumber, int limit);

// ServerResponse viewAllTransactions(String userId, int page);

// ServerResponse viewAccountSummary(String userId);

// ServerResponse getPOSInsight(int page, String search);