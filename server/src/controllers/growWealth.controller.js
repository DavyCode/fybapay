/**
 * Get growth Plans
 * Service requires calling external api for grow wealth
 * Grow wealth will serve as a platform for investment
 * Users can operate with grow wealth from fybapay
 * @param {}
 */

// @flow

import { Request, Response, NextFunction } from 'express';

const getGrowthPlans = async (request: Request, response: Response, next: NextFunction) => {};
