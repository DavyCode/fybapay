/**
 * Process executed on server startup
 */

import { AgentCounter, ServiceSwitch } from '../models';
import { agentCounterInitiator } from '../utils/agentCounter';

export default () => {
  // agentCounterInitiator(AgentCounter);
  // serviceSwitchSeeder(ServiceSwitch);
};
